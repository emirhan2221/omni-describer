wxPython>=4.2.5
# For Gemini API - the app imports both the new SDK (google.genai) and the
# legacy one (google.generativeai.types for HarmCategory/HarmBlockThreshold).
google-genai
google-generativeai
accessible-output2
yt-dlp
# python-vlc bindings for the media player; the libvlc runtime is bundled in
# bin/vlc/ (frozen builds) or taken from a system VLC install (see README).
python-vlc
pyttsx3
pydub
winsdk
simpleaudio
pygame
cryptography==49.0.0
